import { SignInCard } from "@/components/ui/sign-in-card";

/**
 * Role Portal — Renders the premium dark login/signup UI.
 */
export const RolePortal = () => {
  return <SignInCard />;
};

export default RolePortal;